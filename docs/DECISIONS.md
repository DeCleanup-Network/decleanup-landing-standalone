# Decisions

## 2026-05-31 — Cofounder landing feedback (Landing.docx), 17 points

**What changed / where**
- `components/hero.jsx` — removed language block + running marquee; tightened hero vertical rhythm (paddingTop 96→80, grid 56→36, H1 clamp max 168→132px) so the fold fits one screen; KPIs changed from pilot-partners/events to **Cleanups / Weight removed / Weight recycled**; ledger + KPIs now fed by live API with fallback.
- `components/primitives.jsx` — new `useImpactStats()` hook fetching `https://dapp.decleanup.net/api/impact/{global,cleanups}`; header logo swapped to official wordmark; modal copy dropped "three doors".
- `components/sections.jsx` — Farcaster + Full-platform card copy updated; chain pills show only "L2" (logos enlarged), Celo set to L2.
- `components/community.jsx` — footer logo swapped to official wordmark; footer links → mono (`.footer-link`); testimonial pull-quote removed; HEM Japan card link fixed; added Tulum + Moscow sites; motto → "Same network. Pick your door."; Telegram → t.me/decentralizedcleanup.
- `stylesheets/styles.css` — `.nav-link` + `.footer-link` mono treatment; `.chain-pill img` 14→24px; brand-logo theme-toggle rules.
- `index.html` — JSON-LD `sameAs` Telegram fixed.
- `public/brand/` — added `logo-wordmark.png` (640×128, from `assets extra/Logo name icon.png`); refreshed `logo-icon.png` (128px). Sources are huge (~5279px+) so they were downscaled for web.

**Why / alternatives**
- **Live stats with fallback** (not copy-only or deferred): the public impact API is live (CORS-open, returns 200 with `total_cleanups_verified: 0` until mainnet indexes). Hook reads it on mount; `live` flips true only when `total_cleanups_verified > 0`, else shows "—" + "Live · coming Q4" and the ledger falls back to 2024 pilot rows. Auto-goes live when data appears — no redeploy needed.
- **Logo theming**: no black/dark horizontal wordmark asset exists. Dark theme (default, the reviewed one) uses the official white wordmark image; kraft theme falls back to adaptive HTML text. Toggled by CSS on `[data-palette]`. `display` kept in CSS (not inline) so the theme rule can override — inline `display:block` previously defeated it.
- **Motto**: doc gave no replacement; dropped the count to kill the "two doors" (Ecosystem, = 2 chains) vs "three doors" (modal/motto, = 3 entry apps) clash. Ecosystem "Two doors" left as-is (accurate).

**Trade-offs / fragile**
- Live KPI/ledger values can't be visually confirmed until mainnet has verified cleanups (data condition, not code). Verified: fetch 200, field names match, fallback path renders.
- Marquee JSX removed but its drag JS (`app.jsx`) + CSS (`styles.css`) left dormant (guarded on empty `.marquee-wrap`) for easy re-add when Impact Circles launch.
- `IMPACT_LIVE` const + `.pilot-eyebrow` CSS now unused (left in place, harmless).

**Not deployed (left untouched on purpose):** `v1/` and all of `pages/` are in `.vercelignore` and hold stale duplicates of the same strings.

### Second-round fixes (same day, post-review)
- **Chain logos unreadable** — `base-logo-white.svg`/`celo-logo-white.svg` are wide wordmarks (viewBox 100×32). CSS had forced them into 24×24 (square), squishing ~3×. Fixed `.chain-pill img` to `height:24px; width:auto` → renders at correct aspect (75×24), readable.
- **Footer dead text** — replaced the non-clickable `decleanup.net · @DeCleanupNet` line with a labeled **Connect** block of clickable links: Telegram (t.me/decentralizedcleanup), X/Twitter, Farcaster. Gives users a clear way to reach the community (none existed in the footer before).
- **Network-sites lede mismatch** — after adding Tulum + Moscow the section showed 7 sites/5 countries but the lede still said "Five … three countries." Updated to "Seven photo-verified sites across five countries."
- **Collage photos** — pulled HQ originals from Figma; hero → ReFi Phangan beach group (IMG_3929), community-large → bagging action (IMG_4474), captions updated to Koh Phangan/ReFi Phangan. Japan + Nigeria kept (no HQ source).
- Full-design QA via local preview: no broken images, no console errors (only the expected in-browser-Babel warning), mobile + desktop layouts check out.

### Third-round (post-review)
- **Richer community collage** — expanded from 2 photos + stat tile to **5 people-focused photos across 3 countries**, mixing restored originals + new HQ: `testimonial5` (ReFi Phangan group selfie, featured), `testimonial7` (restored HEM Japan overhead group), `testimonial8` (restored Abuja student group), plus existing Koh Phangan action + Nsukka. Rationale (cofounder): "people need to see other people taking part — it's an emotional driver." Stat tile dropped (its data lives in TotalImpact). Collage already stacks 1-col on mobile via existing CSS.
- **Removed all decorative arrows** (→ ↗ ↓) across hero/sections/community/primitives — buttons, links, nav CTA, mobile menu, DMRV flow connectors, the problem→solution glyph. Flagged by user as "AI slop." Checkmark (✓) on the final DMRV step kept. Verified: zero arrow glyphs in rendered text.
- New photo files: `public/testimonial5.jpg` (Figma IMG_4475_1), `testimonial7.jpg`/`testimonial8.jpg` (re-encoded from `.bak.20260531-120000` originals).

### Fourth-round (post-review)
- **Real chain logos** — the old `base-logo-white.svg`/`celo-logo-white.svg` were fake mockups (colored box + system-font text → lowercase "base" vs uppercase "CELO" mismatch). Replaced with real, icon-only marks: `public/base-mark.svg` (official Base symbol, blue circle-with-slot) and `public/celo-mark.svg` (real Celo C-disc, from `celo-celo-logo.svg` + brand fills). Pills now show `[real icon] L2` — no baked text, so no casing inconsistency; chain identity comes from the recognizable mark + card/lede context. `EcoCard` logo `<img>` now has `alt="Base"/"Celo"`.
- **Performance** — hero image (LCP) switched from `loading="lazy"` to `loading="eager" fetchpriority="high" decoding="async"` and added `<link rel="preload" as="image">` in index.html. Below-fold images stay lazy. Recompressed the heaviest collage tile (`testimonial3` 498KB→325KB). Total site images ~2.4MB, only the 369KB hero loads eagerly.
- **SEO/a11y audit (clean)** — single `<h1>`, proper h2/h3 hierarchy, `lang=en`, title/description/canonical/OG/Twitter/JSON-LD all present, every `<img>` has an alt (logo-icon intentionally `alt=""` decorative since the wordmark img carries the name). Mobile verified: hero, KPI grid, eco cards, collage, footer all stack to one column; real logos render.

### Fifth-round — mobile horizontal-clip fix (~360–414px)
- **Symptom** — phone screenshots showed tech-stack, DMRV schematic, and DMRV intro (heading + audience tiles) clipped on the right. Root mechanism: `html,body{overflow-x:clip}` (anti rubber-band guard) **clips** anything wider than the viewport instead of scrolling, so the page never scrolls sideways — content just gets cut. Confirmed via `[...$$('*')].filter(e=>e.getBoundingClientRect().right>innerWidth)` at 390px → 50 clipped elements while `scrollWidth===clientWidth`.
- **Real cause** — grid items default `min-width:auto` (=min-content), so a wide logo / long heading inflates a `1fr` track past the viewport. `.builton-cell` ballooned to 232px (logo `max-width:80%` is a % → does NOT constrain min-content); the inline `repeat(3,1fr)` audience tiles hit ~410px min-content and, because `.dmrv-grid` stacks to one `1fr` column ≤1024px, that inflation cascaded into the schematic card (clipping it too).
- **Fixes** (`stylesheets/styles.css` + 2 JSX): added `img{max-width:100%}` (NO `height:auto` — it overrode the `height=20/24` attrs on the brand wordmark logos and ballooned them to natural 640px); `min-width:0` on `.builton-cell`, `.backers-grid>a`, `.dmrv-grid>*`; converted the two inline `repeat(3,1fr)` grids (audience tiles `sections.jsx`, hero KPIs `hero.jsx`) to `repeat(auto-fit, minmax(min(100%,150px),1fr))` (3-up desktop unchanged, ~2-up phone); lowered heading clamp minimums (`40px→28px` on the DMRV/built-on/steps h2/h3, hero h1 `52px→40px`) so long words fit ~360px.
- **Verified** — local preview (`.claude/preview-server.py`, dcu-root) at 360/390/414px: zero clipped elements except the glossary `.term-tip` (a hidden `opacity:0` tooltip, +11px, no page-scroll impact — left as known-minor). Desktop 1280px unchanged: stack 4-col, dmrv/hero 2-col, tiles/KPIs 3-col, `scrollWidth===clientWidth`. Screenshots captured for all three sections.
- **Gotcha for next time** — the preview harness **locks programmatic scroll** (`scrollTo`/`scrollTop` no-op); to screenshot a lower section, translate `#root` with `transform: translateY(-Ypx)`.
- **Glossary terms on mobile (`Term` in `primitives.jsx`)** — tapping a `?` term jumped to an unrelated part of the page instead of showing the tooltip. Cause: `.term` is a `tabIndex=0` span; on tap the browser focuses it and scrolls the focused element into view — for terms near the top under the fixed nav that's a large downward jump. (Desktop works because the tip shows on hover, no tap/focus.) Fix: `onMouseDown={e=>e.preventDefault()}` on the term — prevents focus-steal/scroll on tap while `onClick` still toggles `data-open` and keyboard Tab focus still works. Also restyled `.term-q` (12px→15px, weight 700, raised `top:-0.28em` so the badge sits off the dotted underline instead of looking crooked) and narrowed the mobile tooltip to `min(220px, calc(100vw-32px))` so a mid-line term's `left:0` tip stays within the viewport. Verified: all 5 terms open in place, no focus-steal, tips within viewport, 0 page overflow at 390px.
- **Glossary follow-up — the above didn't hold on real iOS.** User reported (even in incognito) that tapping a `?` still led nowhere / showed no explanation. Two likely iOS causes: (a) `onMouseDown preventDefault` can cancel the synthesized `click` on iOS → tooltip never opened; (b) the inline `position:absolute` tip + `:hover` sticky-hover behave unreliably on touch. **Reworked the interaction to be touch-bulletproof:** removed `onMouseDown`; tap toggles via `onClick`/`data-open` (+ Enter/Space, `role=button`, `aria-expanded`); close-outside now also listens to `touchstart`; `:hover` trigger gated behind `@media (hover: hover)` so iOS sticky-hover can't fire it; and on `≤640px` the tip becomes a **viewport-pinned fixed bottom sheet** (`position:fixed; left/right:16px; bottom:16px; z-index:60`, arrow hidden) — guaranteed visible on tap regardless of the term's scroll position or any focus-scroll. Confirmed safe because no term has a `transform`/`filter` ancestor that would re-anchor `position:fixed`. Desktop keeps the absolute hover/click tip near the term. Verified in preview (Chromium mobile emulation) at 390px; **real-iOS confirmation pending user re-test.**
- **The actual reason the user saw NO change after two deploys: browser caching.** `index.html` referenced `styles.css` / `*.jsx` as bare URLs, and `vercel.json` served them `Cache-Control: public, max-age=3600, must-revalidate`. Once a phone loaded the page, it reused the cached CSS/JSX for up to an hour **without revalidating** (normal reloads + same-session incognito included) — so neither the restyled `?` nor the new tap logic ever reached the device. Fix: (1) added a `?v=20260601c` cache-busting query to the `styles.css` + 6 `*.jsx` references in `index.html` (new URL ⇒ guaranteed cache miss ⇒ fresh fetch now; `index.html` itself is `max-age=0,must-revalidate` so the new references are seen on the next load); (2) switched the jsx + styles.css `Cache-Control` to `no-cache` in `vercel.json` so future deploys are picked up immediately via ETag revalidation. Also updated `.claude/preview-server.py` to strip `?query` before matching rewrites so local preview still serves the versioned URLs. **Lesson: for this CDN-Babel site, never ship asset changes without either bumping the `?v=` token or relying on the now-`no-cache` headers — bare-URL + long max-age silently hides every edit.**
- **Glossary rebuilt as a modal (the inline-tooltip approach was abandoned).** Even after cache-busting, the inline `position:absolute`/fixed tooltip kept failing on mobile (jump on tap, no visible explanation). Root frustration: it depended on focus/positioning that behaves unreliably on touch. **Final design: clicking a term opens its definition in a centered modal** built on the same `.modal-back`/`.modal` pattern as the working `StartModal`, **rendered via `ReactDOM.createPortal(..., document.body)`** so it's immune to scroll position, focus-scroll, and transformed/overflow ancestors. The term is `role=button` (click + Enter/Space), Escape/backdrop/× all close. The inline `.term-tip` is now a **desktop-hover-only** preview (`@media (hover: hover)`), and is `display:none` on touch (`@media (hover: none)`) so it can't sit off-screen. `data-open`, the click-outside `mousedown`/`touchstart` listeners, and the `onMouseDown preventDefault` were all removed. Verified open/close/center via synthetic clicks + screenshot at 390px (preview is `hover:hover`, so the touch `display:none` path is by-rule, not observed). **Tooling caveat learned: the MCP preview harness locks programmatic scroll and `preview_click` did not dispatch DOM-visible clicks — synthetic `el.click()` is what exercises the real React handler here; never claim a tap-interaction "works" from `withinVP`/`scrollY` checks alone.**
- **ACTUAL ROOT CAUSE (found after the above were all red herrings): the closed mobile nav panel was eating the clicks.** `.nav-mobile-panel` is `display:none` on desktop but `display:block` at ≤860px; when closed it's `opacity:0` + `transform` for the slide animation, but it had **no `pointer-events` override**, so it stayed hit-testable as a `position:fixed` layer covering the top ~617px of the viewport (`top:53px`, tall content). `document.elementFromPoint` at the hero eyebrow returned `a.nav-mobile-link[href="#what"]` — the *invisible* menu link, not the glossary term beneath it. So tapping a `?` actually clicked a hidden menu anchor → smooth-scroll to `#what` (~1514px = "jump to the middle of the page"), and the term's click never fired. This is why **every** earlier glossary fix was invisible (the tap never reached the term) and why desktop was fine (panel is `display:none` there). **Fix (one rule): `.nav-mobile-panel { pointer-events: none }` + `.nav-mobile-panel.open { pointer-events: auto }`.** Reproduced and verified with a REAL `preview_click` on a term that was in-viewport after a fresh load (scrollY stayed 0, no jump, modal opened) — the synthetic `el.click()` had masked it because it doesn't focus/hit-test through overlapping layers. **General lesson: when a tap "does nothing / jumps," check `document.elementFromPoint(x,y)` at the target — an invisible `opacity:0`/`pointer-events:auto` fixed overlay (closed menu/drawer) is a classic click thief; always pair visual-hide with `pointer-events:none`.**

### Sixth-round — local preview server + version control
- **Preview server made threaded.** `.claude/preview-server.py` used single-threaded `socketserver.TCPServer` and hung/died under a normal page load (browser pulling index + 6 jsx + css + images at once) — which made the local preview show stale/frozen content and wasted a long debugging detour. Switched to a `ThreadingTCPServer` subclass (`daemon_threads`, `allow_reuse_address`) and made the rewrite matcher strip `?query`/`#frag` so the new `?v=` cache-busting URLs still resolve. Dev-only file (`.vercelignore`d / git-ignored is not needed since `.claude/launch.json` + this script are committed for reuse, but `settings.local.json` is git-ignored).
- **Put the project under git for the first time.** The folder had no `.git`. Wrote a real `.gitignore` (ignores `*.bak.*` backups [79 of them], `.DS_Store`, `.vercel`, `node_modules`, `.asset-backups-*`, `.claude/settings.local.json`) and pushed to a **new private repo `DeCleanup-Network/decleanup-landing-standalone`** (95 files, ~49 MB). Branch `main`.
- **Discovered two divergent landing codebases.** The org's existing landing repo `DeCleanup-Network/decleanup-network.github.io` is a **Next.js/TypeScript/Tailwind** app — a completely different implementation from this **React-via-CDN + Babel standalone** site (which is what deploys to `decleanup-network.vercel.app` via `vercel --prod` CLI). They are NOT the same project; this standalone was given its own repo rather than overwriting the Next.js one. See KNOWN_ISSUES — a "source of truth" decision is pending.

### Seventh-round — new content pages + investor brief (2026-06-03)
- **Four new pages added & verified.** `litepaper.html`, `tokenomics.html`, `toc.html` (standalone, same React-via-CDN+Babel/styles.css pattern as `sdg.html`) and a `/investors` sub-app (`investors/` — own `investors.css`, jsx, `public/` assets). All four render clean at desktop + mobile (local `dcu-root` preview): no console errors, no broken images, CSS resolves via the `styles.css → stylesheets/styles.css` rewrite.
- **SEO brought to parity with `sdg.html`.** litepaper/tokenomics/toc had only title+description; added robots, canonical, full Open Graph (type/site_name/title/desc/url/image+dims/locale), Twitter summary_large_image, favicons, manifest, theme-color, and JSON-LD (WebPage + BreadcrumbList referencing the home graph's `#organization`/`#website` by @id). investors/index.html gained canonical, og:url/og:image, Twitter card, JSON-LD. JSON-LD validated as parseable on every page.
- **Canonical domain choice.** New canonicals/OG use `https://decleanup-network.vercel.app/...` to stay consistent with the existing repo convention (index.html, sdg.html, sitemap). NOTE the footer historically links litepaper/tokenomics/toc to the custom domain `https://decleanup.net/...` — the vercel.app-vs-decleanup.net canonical question is pre-existing and site-wide; flagged in KNOWN_ISSUES, not resolved here.
- **Performance.** `investors/index.html` was loading React **development** builds; switched to `react.production.min.js` + `react-dom.production.min.js` reusing the exact SRI hashes already vetted in index.html. Babel-standalone in-browser kept (it's the whole site's architecture). Verified the app still mounts on prod React with no errors.
- **sitemap.xml** — added /litepaper, /tokenomics, /toc, /investors; refreshed all lastmod to 2026-06-03.
- **Homepage internal linking** (`components/community.jsx` footer Resources + `components/primitives.jsx` mobile nav panel) — footer now links all four pages (Investor brief added; litepaper/tokenomics/toc switched from `decleanup.net` absolute to repo-relative so they stay on whatever domain serves the page, matching the SDG entry); mobile menu now lists Litepaper, Tokenomics, Theory of change, SDG alignment, Investor brief, GitHub. Bumped the `?v=` cache-bust token in index.html (20260601e → 20260603a) per the no-cache/bump rule.
- **Internal research kept out of the public repo.** Repo is PUBLIC. `docs/research/` is now git-ignored and `investor-research-brief.md` was `git rm --cached` (removed from the repo, kept locally); `investor-research-dossier.md` was never committed. NOTE git history still contains the previously-pushed brief — not scrubbed (would need history rewrite); flag if that matters.

### Eighth-round — canonical domain = decleanup.net + header/footer coherence (2026-06-03)
- **Single canonical domain.** Paul confirmed `decleanup.net` is the main production domain ("all should lead to it"). Replaced every `https://decleanup-network.vercel.app` with `https://decleanup.net` across canonical, Open Graph, Twitter, JSON-LD (@id graph), `sitemap.xml`, and `robots.txt` (8 live files). Legacy `pages/` + `v1/` left untouched (not deployed). Resolves the domain-split flagged in KNOWN_ISSUES.
- **Header coherence.** `sdg.html`'s top nav still had the old section-anchor set (What/How/Ecosystem/Community/Impact → index.html#…); replaced it with the same doc-link nav the other three pages use — `Home · Litepaper · Tokenomics · Theory of Change · SDG` (current page highlighted). All four doc-page headers are now identical.
- **Footer coherence.** Unified the doc-page footers: each now lists the four sibling destinations (the other docs + SDG + Investor brief, excluding self) instead of the previous ad-hoc 0–2 links. Fixed a real bug in the investor memo footer (`investors/primitives.jsx`) where **"SDG alignment" linked to `/toc`** (Theory of Change) — corrected to `/sdg`, and rounded its Protocol column out to Main site · Litepaper · Tokenomics · Theory of change · SDG alignment · GitHub.
- **Verified** in local preview: all pages render, no console errors; sdg nav fits desktop with no overflow; every canonical = decleanup.net/<path>; investor footer SDG → /sdg; doc footers carry the uniform link set.

### Ninth-round — prod fixes: investors blank page, compliance wording, doc cross-linking (2026-06-03)
- **FIXED the blank/black `/investors` page in production.** Root cause: the sub-app's `index.html` loaded its scripts/CSS with **relative** paths (`src="data.jsx"`, `investors.css`) and `img()` returned relative `public/...`. Served at the clean URL `/investors` (no trailing slash, `cleanUrls:true` + `trailingSlash:false`), the browser resolves those against the **site root** → `/data.jsx` 404 → React never mounts → blank page. (Verified live: `www.decleanup.net/data.jsx` = 404, `/investors/data.jsx` = 200.) Locally it worked only because the preview was loaded at `/investors/index.html`. Fix: made all asset paths **absolute** — `/investors/*.jsx`, `/investors/investors.css`, favicon, and `img = (p) => "/investors/" + …`. Now resolves the same regardless of trailing slash.
- **Domain is served on `www.decleanup.net`** (apex `decleanup.net` 301-redirects to www), but canonicals say `https://decleanup.net`. Flagged in KNOWN_ISSUES — fix is a Vercel domain setting (make apex primary so www→apex), not a code change.
- **Compliance wording.** Renamed footer "Invest in token" → "Trade $DCU on Uniswap" (neutral, avoids implying an investment offer). Added a conservative disclaimer ("nothing here is financial/investment/legal advice or an offer/solicitation…") to the homepage footer and to the litepaper + tokenomics footers (token-bearing pages). **Wording is a placeholder — needs lawyer review.**
- **Gardens.fund** footer link: removed "(soon)", pointed to the live garden `https://app.gardens.fund/gardens/42220/0x6068dfc4f2aeca09d8d5845896f3aa76d0fe6960`.
- **Doc cross-linking.** Added "Investors" to the top nav of litepaper/tokenomics/toc/sdg so the investor brief is reachable from every doc page (was footer-only → felt "orphaned").
- **SEO audit (clean).** Every page has exactly one canonical / og:title / og:image / og:url / twitter:card / JSON-LD / description — no duplicates; all og:url match canonical on decleanup.net.
- **Cookies/compliance banner.** Audited the landing code: **no analytics, no cookies, no localStorage, no trackers** are set (only Google Fonts + unpkg CDN, which set no cookies). So a consent banner is **not legally required as-is**. Recommendation pending: add a Privacy Policy + Cookie notice page (footer still shows "Terms (soon) → #") before adding any analytics; the moment a tracker is introduced, a consent banner becomes mandatory (GDPR/ePrivacy).

### Tenth-round — homepage discoverability: Resources strip + Investors band + nav (2026-06-03)
- **Problem (Paul):** litepaper / tokenomics / theory-of-change / investor-brief were only reachable from the footer; investors especially had no presence on the landing.
- **Added two homepage sections** (`components/community.jsx`, rendered in `app.jsx` between `SdgStrip` and `JoinSection`):
  - `ResourcesSection` (`#resources`) — a "Read the protocol" card strip (4 cards: Litepaper, Tokenomics, Theory of Change, SDG), reusing the `.sdg-grid` class so it's 4-up desktop / 2-up mobile.
  - `InvestorsSection` (`#investors`) — a dedicated "For investors & funders / Back impact you can prove." band with two CTAs ("Read the investor brief →" `/investors`, "Talk to the founders" mailto) and the not-an-offer disclaimer inline.
- **Navbar:** added `Docs → #resources` and `Investors → #investors` to `navLinks` (primitives.jsx). 7 desktop nav items verified to fit with no overflow down to 890px (hamburger still takes over ≤860px).
- **Bug caught in review:** the investor heading "…prove." with `maxWidth:720` orphaned the trailing period onto its own line (looked like a stray bullet). Fixed by removing the maxWidth and moving the period inside the gradient `<span>prove.</span>`.
- **Verified** desktop (full + 890px) and mobile (375px): both sections render, cards/CTAs correct, 0 horizontal overflow, no console errors.
